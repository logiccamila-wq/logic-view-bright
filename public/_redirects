# SPA fallback for client-side routing
# Cloudflare Pages handles this automatically, so this file can be empty or removed
# If you need custom redirects, add them here following Cloudflare Pages syntax
